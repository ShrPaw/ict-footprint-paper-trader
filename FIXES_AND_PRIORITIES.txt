═══════════════════════════════════════════════════════════════════
  ICT FOOTPRINT PAPER TRADER — PRIORITIZED FIXES & IMPROVEMENTS
  Generated: 2026-04-03 | Session #10 | After scoring fix + backtests
═══════════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔴 P0 — CRITICAL (must fix before ANY live deployment)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. SIGNAL QUALITY IS TOO LOW — THRESHOLDS NEED TO BE MUCH HIGHER
   The scoring fix (removing weight normalization) revealed the
   system's true signal quality. Old thresholds (0.65-0.78) let
   too many marginal signals through.

   Evidence: ETH backtest with fixed scoring:
   - 1,087 trades, PF 0.77, WR 48.5%, -$2,314 PnL
   - stop_loss: 3% WR, -$16,869 (noise trap)
   - breakeven_sl: 0% WR, -$803 (scratching recoverable trades)
   - trailing_sl: 78% WR (THE edge, but only 504 of 1087 trades reach it)

   The problem: weak signals enter at bad prices → never reach
   trailing activation (0.9-1.2 ATR) → get stopped or scratched.

   Action: Raise thresholds to only allow STRONGEST signals:
   - ETH: minConfluenceScore 0.65→0.80, minSoloScore 0.78→0.90
   - SOL: minConfluenceScore 0.58→0.75, minSoloScore 0.72→0.85
   - BTC: minConfluenceScore 0.62→0.78, minSoloScore 0.78→0.88

   Expected: 60-70% fewer trades, but each trade is higher quality.
   More trades reach trailing activation → more trailing_sl wins.

2. BREAKEVEN IS A LOSING MECHANIC — KILL IT OR RAISE ACTIVATION
   Breakeven at 0.6-0.8 ATR scratches trades at 0% WR.
   -$803 in ETH backtest. These trades moved in our favor slightly
   then reversed — breakeven kills them instead of letting trailing
   take over.

   Options:
   A) Remove breakeven entirely — let trailing handle everything
   B) Raise activation to 1.5 ATR (above trailing activation at
      0.9-1.2 ATR) — only protect trades that are deep in profit

   Recommended: Option A for simplicity. Trailing at 0.9-1.2 ATR
   already protects profitable trades. Breakeven adds nothing but
   premature exits.

3. XRP KILLED — DONE
   Removed from config.symbols, ecosystem.config.cjs, package.json.
   WFA proof: 33% OOS profitable, PF 0.92.
   Code preserved for future revalidation.

4. SCORING FIX — DONE
   Live DaytradeMode._scoreSignal() was applying weight twice.
   Fixed in both live and backtest to use raw confidence values.
   Both now on identical scoring scale.

5. LIVE BOT RISK SIZING FIX — DONE
   BinanceLiveBotRunner and LiveBotRunner now apply per-asset
   riskMultiplier from assetProfiles.js. Previously used flat
   global riskPercent for all assets.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🟡 P1 — HIGH IMPACT (significant PnL improvement expected)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

6. CONSIDER REMOVING REGULAR STOP LOSS ENTIRELY
   stop_loss has 3% WR and -$16,869 in ETH backtest.
   Even at 2.0 ATR width, it's a noise trap.

   Radical option: Remove regular SL. Let every trade run until
   trailing SL or partial TP catches it. Risk is a few trades
   might go deep into drawdown, but -$16K in noise traps would
   be eliminated.

   Implementation: In PaperEngine.checkExits(), only check SL
   if trailingActive or breakevenTriggered. Initial "SL" becomes
   a max-loss-only circuit breaker (e.g., 5 ATR = emergency exit).

   Expected impact: PnL improvement of $10-15K over 4 years
   if even 50% of SL exits would have recovered into trailing.

7. FEE OPTIMIZATION — MAKER ORDERS
   Current: $1,983 fees on ETH alone over 4 years.
   Taker fee: 0.05% | Maker fee: 0.02%

   Action: Use limit orders at mid-price ± small offset.
   Model 70% fill rate for conservative estimate.
   Expected: fees drop ~60%. Adds ~$4K-5K to PnL per asset.

8. RAISE SIGNAL THRESHOLDS PER REGIME
   Not all regimes are equal. VOL_EXP signals should have a
   HIGHER bar than TRENDING_UP (which already gets a 1.3x boost).

   Action: Add regime-specific threshold multipliers:
   - VOL_EXPANSION: 1.0x (baseline)
   - TRENDING_UP: 0.85x (lower bar — ICT works better in trends)
   - RANGING: 1.2x (higher bar — only best setups in chop)

9. BTC DECISION — THIN EDGE, HIGH FEES
   BTC PF 1.10 in previous backtest. With fixed scoring + wider
   stops, needs fresh validation. If PF < 1.2 after fixes,
   consider dropping BTC for initial live deployment.

   Recommendation: Run BTC backtest with new settings. If PF < 1.2,
   start live with ETH + SOL only.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🟢 P2 — MEDIUM IMPACT (quality of life + robustness)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

10. WALK-FORWARD ANALYSIS
    All parameter tuning so far has been in-sample. Need WFA
    to validate edge is real, not curve-fitted.

    Action: `npm run walkforward:optimize` with new thresholds.

11. CONFIDENCE CALIBRATION STANDARDIZATION
    Precompute.js and ICTAnalyzer.js both compute OB/sweep/OTE
    confidence. They should produce identical values for the same
    input. Verify and document the calibration formulas.

12. REGIME-DEPENDENT SLIPPAGE — DONE
    Added slippageByRegime to config.js. VOL_EXPANSION gets
    0.015% (wide), RANGING gets 0.003% (tight).

13. DAILY TRADE LIMIT — DONE
    maxDailyTrades: 2 enforced in PaperEngine and backtest.

14. SESSION HARD-GATING — DONE
    BTC/XRP (when active) only trade during NY + overlap.

15. ADD CONFIG VALIDATION ON STARTUP
    No validation that config values make sense. Typos could
    silently break things.

    Action: Add validate() method checking:
    - All symbols have profiles
    - Risk multipliers are positive and sane
    - Blocked regimes are valid names

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔵 P3 — NICE TO HAVE (polish + monitoring)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

16. PERFORMANCE ALERTING
    Alert if: 5 consecutive losses, daily PnL < -2%, bot offline.

17. EQUITY CURVE TO TELEGRAM
    Daily summary is text-only. Add equity sparkline.

18. METRICS EXPORT TO CSV (live)
    Write trades to data/live-trades-{coin}.csv on each close.

19. DEPLOY SCRIPT — DONE
    GITHUB_TOKEN support for private repo cloning.

20. DASHBOARD TRADE HISTORY — DONE
    Added GET /api/bots/:name/trades endpoint.

═══════════════════════════════════════════════════════════════════
  SUMMARY — EXECUTION ORDER
═══════════════════════════════════════════════════════════════════

  Phase 1 (TODAY):
    1. ✅ Scoring fix (live ↔ backtest parity)
    2. ✅ Live bot risk sizing fix
    3. ✅ Kill XRP
    4. ✅ Wider stops (2.0 ATR)
    5. → Run backtests with MUCH higher thresholds (0.80-0.90)
    6. → Test removing breakeven entirely

  Phase 2 (this week):
    7. Test removing regular SL (trailing-only)
    8. Maker order implementation
    9. BTC go/no-go decision
    10. Walk-forward analysis

  Phase 3 (before live):
    11. Final parameter lock
    12. Config validation
    13. Testnet dry run (1 week)

  Phase 4 (post-live):
    14-18. Monitoring, alerts, polish

═══════════════════════════════════════════════════════════════════
  CRITICAL LESSON LEARNED
═══════════════════════════════════════════════════════════════════

  The old "validated" results (Session #8: PF 1.20, +$3,392) were
  partly an artifact of the scoring mismatch. The backtest was
  normalizing by weight (dividing confidence by 0.35/0.65), which
  inflated scores and made thresholds easier to pass selectively.

  The REAL system (with consistent scoring) shows:
  - Signal quality is lower than thought
  - The edge exists (trailing 78% WR) but is FRAGILE
  - It only works when entries are high-quality enough to reach
    trailing activation (0.9-1.2 ATR profit)

  Solution: MUCH higher signal thresholds. Trade less, trade better.
  ~15-20 trades/month (not 25-30) with 60%+ WR is the target.

═══════════════════════════════════════════════════════════════════
