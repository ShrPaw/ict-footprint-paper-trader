# SESSION CONTEXT — 2026-03-30 (GMT+8)
# Last session: 07:04 - 07:54
# Status: STUCK — VM SSH key not added yet

## CURRENT SITUATION

The VM is RUNNING but we can't get in. SSH port 22 responds from the external IP
(basic connectivity test passed), but the public key was never added to the VM.

## VM DETAILS (latest)

- VM Name: ict-trader
- Zone: us-central1-c
- Status: RUNNING ✅
- External IP: 35.224.22.119 (CHANGED from 35.202.132.118 after stop/start)
  ⚠️ IP changes every time VM is stopped and started! Consider reserving a static IP.
- Internal IP: 10.128.0.2
- Machine: e2-medium (2 vCPU, 4GB RAM)
- OS: Ubuntu 22.04 LTS

## FIREWALL STATUS — ALL GOOD ✅

Verified on VPC Network → Firewall rules page:
- allow-ssh-all: tcp:22 from 0.0.0.0/0, targets all instances ✅
- default-allow-ssh: tcp:22 from 0.0.0.0/0, targets all instances ✅
- No firewall rules for port 3500 (dashboard) yet — need to add tcp:3500

## SSH KEY STATUS — NOT ADDED ❌

A key pair exists on the assistant's machine:
- Private: /root/.ssh/id_ed25519_ict
- Public: /root/.ssh/id_ed25519_ict.pub

Public key content (for adding to VM):
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPWSK1KKfJjXRvhXV8NYL8PPH0wJG3vwfvwdPclzEoNQ ict-trader

## BLOCKER — HOW TO ADD SSH KEY

The user was guided to:
1. Go to Compute Engine → VM instances → click "ict-trader" (the name)
2. Click "EDIT" (blue button at top)
3. Scroll down to "SSH Keys" section
4. Click "+ Add item"
5. Paste the public key line above
6. Click SAVE

User got stuck — was editing the VM but couldn't find the SSH Keys section.
The page has these sections (top to bottom):
- Name, Zone, Machine type, GPU, Display device
- Availability policies
- Confidential VM service
- Deletion protection
- **Remote access** — has "Block project-wide SSH keys" setting
- **SSH Keys** — this is where to add the key (might need to scroll further)

ALTERNATIVE: If user has gcloud CLI installed locally:
```bash
gcloud compute instances add-metadata ict-trader \
  --zone=us-central1-c \
  --metadata="ssh-keys=ict-trader:ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPWSK1KKfJjXRvhXV8NYL8PPH0wJG3vwfvwdPclzEoNQ ict-trader"
```

ALTERNATIVE 2: Use GCP Console → Compute Engine → Metadata → SSH Keys
(adds key to ALL VMs in the project, not just ict-trader)

## WHAT WE WERE DOING (end of session)

User was on the VM Edit page, scrolling to find SSH Keys section.
Session ended before key was added. Next session: help user add the SSH key,
then assistant can SSH in and verify PM2 bots, .env, dashboard, etc.

## NEXT STEPS (after SSH key is added)

1. SSH in: ssh -i /root/.ssh/id_ed25519_ict root@35.224.22.119
2. Check PM2 status: pm2 list
3. Check .env file (needs TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID)
4. Check if bots are running: pm2 logs
5. Check dashboard: curl localhost:3500
6. Add firewall rule for port 3500 (dashboard access)
7. Consider reserving a static IP to stop IP changes on stop/start
8. Fix known bugs (config.js symbols, ADX smoothing, state persistence)
