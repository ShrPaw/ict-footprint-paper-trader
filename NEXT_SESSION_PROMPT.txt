## NEXT SESSION PROMPT — Quant Research Phase 2 Redesign
## Date: 2026-04-04
## Status: EDGE REJECTED — Return to Phase 2

---

You are continuing the Quant Research System (ROS) for the ict-footprint-paper-trader project.

### WHERE WE ARE

Phase 1 (Data Validation) ✅ — 691,200 candle-hours acquired across BTC/ETH/SOL/XRP × 4 timeframes

Phase 2 (Event-Level Edge Discovery) ✅ — 9 event types tested, VWAP deviation emerged as candidate

Phase 2.5 (Stress Testing) ✅ — EDGE REJECTED

### WHAT HAPPENED

The below-VWAP mean reversion edge FAILED stress testing:

- **Regime instability**: Edge doesn't persist across bull/bear/sideways (0-60% of sub-regimes significant)
- **Timing fragility**: Collapses at +3 candle entry delay
- **Subsampling collapse**: Only 1-53% of random subsamples remain significant
- **Directional symmetry**: SOL above-VWAP actually outperforms below-VWAP; XRP both directions equal

Root causes: cluster inflation (72-76% events in clusters), regime pooling artifact, temporal autocorrelation.

### WHAT TO DO NEXT

Return to Phase 2 with DIFFERENT event definitions. The VWAP deviation family is dead.

Recommended directions:
1. **Momentum/breakout events** — trend-aligned signals may be more robust in crypto
2. **Volatility regime transitions** — expansion/compression cycle events
3. **Microstructure events** — funding rate anomalies, open interest shifts, volume spikes
4. **Conditional events** — VWAP deviation + specific regime filter (not standalone)

### CODEBASE

All research code is in `quant_research/`:
- `data_acquisition.py` — fetches Binance OHLCV data
- `phase2_analysis.py` — event detection + distribution analysis
- `phase3_validation.py` — random baseline + shuffle tests
- `phase45_analysis.py` — time modeling + loss forensics
- `stress_tests.py` — the 6 adversarial stress tests
- `results/` — all JSON results and text reports

Raw data is in `quant_research/raw_data/` (not committed, re-fetch with `data_acquisition.py`).
Python venv: create with `python3 -m venv quant_env && pip install pandas numpy scipy requests matplotlib`

### RULES

- Do NOT reuse VWAP deviation as an event definition
- Do NOT optimize parameters before edge discovery
- Must run stress tests BEFORE declaring any edge valid
- Assets must be analyzed independently (no combining)
