# ICT + Footprint Paper Trader — Full Project Context
# Last updated: 2026-03-29
# This file is the single source of truth for project state.

## WHAT THIS IS

A regime-adaptive paper trading engine for crypto perpetuals.
Uses ICT (Inner Circle Trader) concepts on 1H candles combined with order flow footprint analysis.
Built in Node.js with CCXT. Trades on Hyperliquid (live market data) and backtests on Binance (historical).
Paper trading only — no real money at risk.

## VERSION: v1.2.2

## VALIDATED RESULTS (Jan-May 2025, Binance backtest)

| Asset | PnL     | PF   | WR    | Trades | Max DD | Sharpe | Regime Edge           |
|-------|---------|------|-------|--------|--------|--------|-----------------------|
| ETH   | +$684   | 1.52 | 59.3% | 54     | 3.5%   | 1.18   | RANGING + VOL_EXP     |
| SOL   | +$1,426 | 1.72 | 59.7% | 77     | 5.5%   | 1.71   | RANGING only          |
| BTC   | +$317   | 1.53 | 65.4% | 26     | 2.9%   | 0.85   | VOL_EXPANSION only    |
| XRP   | +$1,242 | 2.27 | 65.9% | 44     | 4.1%   | 2.57   | VOL_EXPANSION only    |
| Total | +$3,669 | ~1.75| 61.8% | 201    | —      | —      |                       |

Key insight: BTC and XRP are the INVERSE of SOL. They thrive in VOL_EXPANSION, die in RANGING.
SOL thrives in RANGING, breakeven in VOL_EXPANSION. ETH works in both.

## PER-ASSET REGIME FILTERING (THE EDGE)

| Asset | RANGING     | VOL_EXPANSION | TRENDING_DOWN | LOW_VOL |
|-------|-------------|---------------|---------------|---------|
| ETH   | ✅ +$334    | ✅ +$350      | ❌ blocked    | ❌ blocked |
| SOL   | ✅ +$1,397  | ✅ +$29       | ❌ blocked    | ❌ blocked |
| BTC   | ❌ blocked  | ✅ +$317      | ❌ blocked    | ❌ blocked |
|       | (-$728)     |               |               |         |
| XRP   | ❌ blocked  | ✅ +$1,242    | ❌ blocked    | ❌ blocked |
|       | (-$282)     |               |               |         |

## ARCHITECTURE

```
bots/
  BotRunner.js          — Shared bot runner (per-asset config, dashboard reporting)
  eth/bot.js            — ETH entry point (port 3451)
  sol/bot.js            — SOL entry point (port 3452)
  btc/bot.js            — BTC entry point (port 3453, $5k balance, research mode)
  xrp/bot.js            — XRP entry point (port 3454, $5k balance, research mode)
dashboard/
  server.js             — Dashboard API server (port 3500)
  index.html            — Web UI (dark theme, auto-refresh 3s)
engine/
  main.js               — Legacy multi-asset trader (NOT USED with bots)
  PaperEngine.js        — Orders, PnL, trailing stops, partial TP, time_exit
  backtest.js           — Historical backtester with regime/signal/exit breakdowns
strategies/
  DaytradeMode.js       — THE strategy: 1H ICT + trend + footprint confluence
  ModeRouter.js         — Routes to daytrade (weekend/scalping disabled)
  WeekendMode.js        — Disabled (overtrading)
  ScalpingProMode.js    — Disabled (no edge on 15m)
  StrategyEngine.js     — Original v0.5 engine (legacy, unused)
analysis/
  RegimeDetector.js     — TRENDING_UP/DOWN, RANGING, VOL_EXP, LOW_VOL, ABSORPTION
  ICTAnalyzer.js        — FVG, Order Blocks, OTE, Liquidity Sweeps, BOS
  RealFootprintAnalyzer.js — Delta divergence, absorption, POC, stacked imbalance
  FootprintAnalyzer.js  — Estimated footprint (legacy, not used by daytrade)
config/
  assetProfiles.js      — Per-asset: blockedRegimes, risk, SL tightness, weights
config.js               — Master config: symbols, risk, ICT params, engine settings
data/
  HyperliquidFeed.js    — Real-time candles + trade-level footprint from Hyperliquid API
  DataFeed.js           — CCXT fallback (not used)
  TradingViewWebhook.js — External signal receiver
alerts/
  TelegramAlerter.js    — Telegram notifications (entries, exits, regime, daily summary)
```

## HOW TO RUN

```bash
# Install
npm install
cp .env.example .env
# Edit .env with TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID

# Backtest per asset
npm run backtest:eth
npm run backtest:sol
npm run backtest:btc
npm run backtest:xrp

# Run single bot
npm run bot:eth

# Run all bots + dashboard (development)
npm run dashboard & npm run bots:all

# Run with PM2 (production)
npm run pm2:start
pm2 logs
pm2 monit
```

## DEPLOYMENT

See deploy.sh for one-command VPS setup:
1. Ubuntu VPS (Hetzner CX22 recommended, €4.15/mo)
2. bash deploy.sh
3. Edit .env with Telegram creds
4. pm2 restart all

Dashboard at http://YOUR-IP:3500

## KNOWN ISSUES

1. RegimeDetector uses simplified ADX (not Wilder's smoothing) — values may be off
2. Footprint estimation from OHLCV is crude (close position = buy/sell ratio)
3. BTC/XRP only validated in VOL_EXPANSION — fragile if market shifts
4. No live execution validation yet (all results are backtested)
5. Jul-Nov 2024 bull run period loses (PF 0.69 ETH, 0.96 SOL)
6. No state persistence — bot crash = lost trade history
7. Dashboard has no auth — open to anyone who finds port 3500

## NEXT STEPS (Priority Order)

1. Fix config.js symbols inconsistency (README says 4 assets, config has 2)
2. Implement proper ADX (Wilder's smoothing) in RegimeDetector
3. Walk-forward validation (train 3mo, test 1mo, slide through data)
4. Deploy to VPS and run live paper trading for 2 weeks
5. Add state persistence (JSON dump every minute)
6. Dashboard auth (basic HTTP auth or IP whitelist)
7. Test on 2023-2024 data for more robustness
8. Monte Carlo simulation on trade sequence
9. Unit tests for RegimeDetector
10. Clean up unused WeekendMode/ScalpingProMode code

## GIT HISTORY

- c77f69c v1.2.2: PM2 config + one-click deploy script
- 101cd62 v1.2.1: Web dashboard for live bot monitoring
- 99ef521 v1.2.0: Per-asset bot architecture + regime filtering
- 7ad20d7 v1.1.0: README + DevLog updated with validated results

## DEPENDENCIES

- ccxt ^4.4.0 (exchange data)
- ws ^8.18.0 (websocket, imported but not actively used)
- chalk ^5.3.0 (terminal colors, imported but not actively used)
- dayjs ^1.11.10 (date utils, imported but not actively used)
